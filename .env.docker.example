# =============================================================================
# MF-Investments Docker Environment Configuration
# =============================================================================
# Copy this file to .env.docker and customize for your deployment
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
DB_ROOT_PASSWORD=change_me_root_password_2024
DB_NAME=mf_selection_app
DB_USER=mf_user
DB_PASSWORD=change_me_user_password_2024

# -----------------------------------------------------------------------------
# Application Security
# -----------------------------------------------------------------------------
# IMPORTANT: Generate a secure random string for production!
# Use: openssl rand -hex 32
JWT_SECRET=change_this_to_a_secure_random_string_at_least_32_chars

# -----------------------------------------------------------------------------
# Port Configuration
# -----------------------------------------------------------------------------
# APP_PORT: Backend API port (default: 4000)
# WEB_PORT: Nginx web port (default: 80, only if using nginx profile)
APP_PORT=4000
WEB_PORT=80

# -----------------------------------------------------------------------------
# Feature Toggles
# -----------------------------------------------------------------------------
# Enable scheduled tasks (SIP/STP/SWP execution at 6 AM daily)
ENABLE_SCHEDULER_CRON=true

# Enable nightly full sync of mutual fund data (2 AM IST)
ENABLE_FULL_SYNC=true

# Enable incremental NAV updates during market hours
ENABLE_INCREMENTAL_SYNC=true
